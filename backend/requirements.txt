# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# --- LLM providers ---
# Primary: Google Gemini. Secondary/fallback: Groq (GroqCloud).
google-genai>=1.0
groq>=0.11

# --- Database (added in step 5) ---
sqlmodel>=0.0.22
psycopg2-binary>=2.9.9

# --- Observability ---
sentry-sdk[fastapi]>=2.0

# --- Scheduler + HTTP client for the daily job (added in step 7) ---
apscheduler>=3.10,<4.0
httpx
